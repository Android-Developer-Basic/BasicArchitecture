<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Состояние для активной кнопки (включенной) -->
    <item android:state_enabled="true" android:drawable="@color/button" />

    <!-- Состояние для неактивной кнопки (выключенной) -->
    <item android:state_enabled="false" android:drawable="@android:color/darker_gray" />
</selector>